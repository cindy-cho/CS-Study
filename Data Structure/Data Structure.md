Data Structure
=============
Array
-----
* 개념

* 시간 복잡도

* 동작

* 장점

* 단점

* 특징

Linked List
-----------
* 개념

* 시간 복잡도

* 동작

* 장점

* 단점

* 특징

HashTable : (Key, Value)로 데이터를 저장하는 자료구조
---------
* 개념
    * Key와 Value를 1:1로 matching 하여 저장하는 자료구조
    * Key
        * 고유한 값
        * Hash Function을 이용해 Hash로 변경 후 저장한다
    * Hash
        * Hash Function의 결과
        * 저장소에서 Value와 매칭되어 저장

* 시간 복잡도
    |     | avg |worst|
    |-----|-----|-----|
    | 탐색 | O(1)| O(N)|
    | 삽입 | O(1)| O(N)|
    | 삭제 | O(1)| O(N)|

* 동작
    * Hash Funtion을 이용하여 Key값을 Hash값으로 바꾼다
    * Hash 값을 Index로 하여 Value를 저장

* 장점
    * 적은 리소스로 많은 데이터를 효율적으로 관리
    * 빠른 검색, 삽입, 삭제 : O(1) - 배열의 인덱스 사용
    * 보안에 유리 - Key와 Hash
    * 중복 제거에 유용
    * 데이터 캐싱에 주로 사용

* 단점
    * 충돌 발생 가능성
    * 공간 복잡도 증가
    * 순서 무시
    * 해시 함수 의존

* Hash Table :: Hash Map
    * 공통점
        * (Key, Value) 구조
    * Hash Table
        * 동기
        * NULL 값 허용
        * 보조 Hash Fucntion과 Seperating Chaining을 사용하여 해시 충돌 적음
    * Hash Map
        * 비동기 - 멀티 스레드 환경에서 유의
        * NULL 값 미 허용 : hashcode(), equals() 사용하기 때문

* Hash 충돌
    * Key의 Hash값이 중복 되는 경우
    * 중복이 많을수록 탐색 시간 복잡도 증가 : O(1) -> O(N)

* Hash 충돌 해결방법
    * Seperating Chaining (분리 연결법)
        * Linked List, Red-Black Tree
        * 동일한 Hash Bucket에 대해서 추가 메모리를 사용하여 저장
        * 장점 : 해시 테이블 확장 없이 간단하게 구현 가능
        * 단점 : 데이터의 수가 많아지면 효율성이 감소
    * Open Addressing (개방 주소법)
        * 비어있는 공간을 활용하는 방법
        * Linear probing, Quadratic Probing, Double Hashing Probing
        
Stack : LIFO 형식의 자료구조
-----
* 개념
    * 마지막에 들어온 데이터를 먼저 빼주는 LIFO 형식의 자료구조

* 시간 복잡도

* 장점

* 단점

* 사용 사례
    * 재귀 알고리즘 : 스택 프레임에 데이터 저장
    * 웹 브라우저 방문 기록 : 뒤로가기
    * 실행 취소
    * 역순 문자열 만들기
    * 수식 괄호 검사
    * 후위 표기법 계산

Queue : FIFO 형식의 자료 구조
-----
* 개념
    * 먼저 들어간 데이터가 먼저 나가는 FIFO 형식의 자료구조

* 시간 복잡도

* 장점

* 단점

* 종류
    * Priority Queue(우선 순위 큐)
        * 데이터들이 우선 순위를 가지고 있으며, 우선순위가 높은 데이터가 먼저 나간다.
        * 사용 사례
            * 시뮬레이션 시스템
            * 네트워크 트래픽 제어
            * 운영체제에서의 작업 스케쥴링

* 사용 사례
    * BFS 구현
    * 캐시 구현
    * 우선순위가 같은 작업 예약
    * 프린터의 출력 처리
    * 콜센터 고객 대기시간
    * 윈도우 시스템의 메시지 처리기

Graph : 노드와 간선을 하나로 모아놓은 자료구조
-----
* 개념
    * 노드(Node)와 간선(Edge)를 모아놓은 자료구조

* 시간 복잡도

* 특징
    * 네트워크 모델
    * 단방향 / 양방향
    * Self-loop / loop / circuit
    * Cyclic / Acyclic

* 장점

* 단점

* 사용 사례
    * 지도, 지하철 노선도
    * 선수 과목

Tree : 부모, 자식 관계로 이루어진 자료구조
----
* 개념

* 시간 복잡도

* 특징
    * 트리는 cycle이 존재할 수 없다
    * 계층 모델
    * DAG(Directed Acyclic Graph) : 그래프의 한 종류이다
    * 노드가 N개인 트리는 항상 N-1개의 간선을 가진다
    * 루트에서 어떤 노드로 가는 경로는 유일하다
    * 모든 자식 노드는 한 개의 부모 노드만을 가진다

* 동작
    * 순회 방법
        * Pre-order
        * In-order
        * Post-order

* 장점

* 단점

* 종류
    * 이진 트리
    * 이진탐색 트리
    * 균형 트리
    * 이진 힙


Heap : 완전 이진 트리의 일종, 우선순위 큐에 사용
-----------
* 개념
    * 여러 개의 값들 중 최댓값이나 최솟값을 빠르게 찾아내도록 만들어진 자료구조
    * 반정렬 상태 (느슨한 정렬 상태)를 유지한다
        * 부모 노드의 키 값이 자식 노드의 키 값 보다 항상 작다
    * 중복된 값을 허용

* 시간 복잡도

* 동작
    * 저장하는 표준 자료구조는 배열
    * 구현을 위해 첫번째 인덱스(0번)은 사용하지 않는다
    * 인덱스 계산 방법
        * 왼쪽 자식의 인덱스 = 부모의 인덱스 * 2
        * 오른쪽 자식의 인덱스 = 부모의 인덱스 * 2 + 1
        * 부모의 인덱스 = 자식의 인덱스 / 2
    * 삽입
        * 새로운 노드를 힙의 마지막 노드 다음에 삽입한다
        * 부모와 자식의 관계를 힙의 성질에 따라 재정렬한다
    * 삭제
        * 루트 노드를 삭제한다
        * 삭제된 자리에 힙의 마지막 노드를 넣어준다
        * 부모와 자식의 관계를 힙의 성질에 따라 재정렬한다

* 장점

* 단점

* 종류
    * Max Heap (최대 힙)
        * 부모 노드의 키 값이 자식 노드의 키 값보다 크거나 같은 완전 이진 트리
    * Min Heap (최소 힙)
        * 부모 노드의 키 값이 자식 노드의 키 값보다 작거나 같은 완전 이진 트리

Red-Black Tree
--------------
* 개념

* 시간 복잡도

* 동작

* 장점

* 단점

* 특징

B+ Tree
-------
* 개념

* 시간 복잡도

* 동작

* 장점

* 단점

* 특징